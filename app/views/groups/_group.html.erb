<div id="<%= dom_id(group) %>">
  <div class="sm">
    <i class="bi bi-<%= group.icon %>"></i>
  </div>
  <div class="group-list-item">
    <h3><%= group.name %></h3>
    <p>View your Total spending</p>
  </div>
</div>